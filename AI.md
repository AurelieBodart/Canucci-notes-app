prompt : Create a React/Typescript/Vite project called "Canucci-notes-app" with basic config files, tailwind css, one page called NotesPage with basic empty structure and one component called NoteForm with basic empty structure. 
Add a proper .gitignore, README.md with setup and run instructions
Use npm as package manager

prompt : Generate a list view of notes, with note.content, a edit pen icon button and a trash icon button for each item

comment :
Most of the views are generated with a prompt asking to Cursor to show a button, create form, add the checkbox, an icon to save time, have a nice tailwind css and focus on the actions, states and validations